{{!-- {{log .}} --}}
<div class="flex flex-col gap-2">
    {{#each items as | item |}}
    {{#each item.system.defenses as | defense |}}
    <div
        class="grid grid-cols-[1fr_1fr_6fr_4fr_3fr] gap-1 border-[1px] dark:border-slate-200 dark:text-slate-200 rounded-none m-0 p-[2px] text-xs leading-4">
        <input class="!my-0" type="checkbox" id="defense" value="{{defense.hitsStopped}}" checked>
        <img src="{{item.img}}" class="w-[1.5em] h-[1.5em]">
        <div>{{item.name}}</div>
        <div>{{defense.action}}</div>
        <div>Stops {{defense.hitsStopped}}</div>
    </div>
    {{/each}}
    {{/each}}
</div>
</div>